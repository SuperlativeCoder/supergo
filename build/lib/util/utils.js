const path = require('path')

const config = require('../../../supergo.config')

exports.assetsPath = function (mode, path) {
  // var assetsSubDirectory = config.client[mode].assertPath
  // return path.join(assetsSubDirectory, path)
  return ''
}

exports.cssLoader = function (options) {
  
}